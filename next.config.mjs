// This is an example of how to extend Next.js configuration
module.exports = {
  // ... existing configuration ...
  // Add the following to the existing configuration
  experimental: {
    externalDir: true,
  },
  // ... existing configuration ...
}